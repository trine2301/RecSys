package recsys.controller;

import io.micronaut.http.annotation.Controller;

/**
 * A controller-class that handles HTTP POST requests at the /transaction/compare endpoint.
 * It uses a TransactionComparison
 */
@Controller("/files")
public class TransactionComparisonController {
}
