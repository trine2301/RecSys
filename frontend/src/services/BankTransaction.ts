
/**
 * Type representing a bank transaction.
 * Each transaction includes an ID, date, amount and description.
 */
export type BankTransaction = {

  id: string,
  date: string, //Date ?
  amount: number,
  description: string
}
